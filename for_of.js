// First Example
const colors = ['red', 'green', 'blue'];

for (let color of colors) {
  console.log(color);
}

// Second Example
const numbers = [1, 2, 3, 4, 5];
let total = 0;

for (let number of numbers) {
  total += number;
}

console.log(total);
